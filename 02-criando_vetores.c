// Como criar variáveis e vetores:
// O que é um vetor? Um conjunto de variáveis do mesmo tipo de dados armazenados em um bloco contíguo na memória do computador e são acessados ​​por meio de um índice.
// Tipos de variáveis: Int = Inteiro | Char = Caractere | Float = Real
// Identificador ou ID da variável = Nome da variável, Ex.: Idade
int main() {
    int idade;
    char sexo;
    float nota;
    return 0;
}